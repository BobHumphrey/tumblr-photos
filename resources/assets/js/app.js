$(function () {
  $('#membership_date').datetimepicker({
    format: 'MM-DD-YYYY'
  });
  $('#posted_date').datetimepicker({
    format: 'MM-DD-YYYY'
  });
  $('#submitted_date').datetimepicker({
    format: 'MM-DD-YYYY'
  });
  $('#published_date').datetimepicker({
    format: 'MM-DD-YYYY'
  });
});


